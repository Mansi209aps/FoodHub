import React from 'react'
import { Navbar } from '../Shared/Navbar'

const Menu = ({ product, onAddToCart }) => {
    return (
        <div>
            <Navbar />

        </div>
    )
}

export default Menu